INSERT INTO user_roles(
	role_name,
	user_id,
	target_team_slug
) VALUES (
	'Team member',
	(SELECT id FROM users WHERE email = 'authenticated@example.com'),
	'member'
), (
   	'Team owner',
   	(SELECT id FROM users WHERE email = 'authenticated@example.com'),
   	'owner'
);

RETURNS

[]
