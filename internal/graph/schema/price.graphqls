extend type Query {
	# Get current price for the given type.
	currentUnitPrice(resourceType: ResourceType!): Price!
}

type Price {
	description: String!
	price: Float!
	unit: String!
	currency: String!
	type: ResourceType!
}

enum ResourceType {
	CPU
	MEMORY
}
