// Code generated by sqlc. DO NOT EDIT.
// source: department.sql

package departmentsql

import (
	"context"

	"github.com/nais/api/internal/slug"
)

const count = `-- name: Count :one
SELECT
	COUNT(*)
FROM
	departments
`

func (q *Queries) Count(ctx context.Context) (int64, error) {
	row := q.db.QueryRow(ctx, count)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const create = `-- name: Create :one
INSERT INTO
	departments (slug, purpose, slack_channel)
VALUES
	($1, $2, $3)
RETURNING
	id, created_at, slug, purpose, slack_channel
`

type CreateParams struct {
	Slug         slug.Slug
	Purpose      string
	SlackChannel string
}

func (q *Queries) Create(ctx context.Context, arg CreateParams) (*Department, error) {
	row := q.db.QueryRow(ctx, create, arg.Slug, arg.Purpose, arg.SlackChannel)
	var i Department
	err := row.Scan(
		&i.ID,
		&i.CreatedAt,
		&i.Slug,
		&i.Purpose,
		&i.SlackChannel,
	)
	return &i, err
}

const list = `-- name: List :many
SELECT
	id, created_at, slug, purpose, slack_channel
FROM
	departments
ORDER BY 
	slug ASC
LIMIT
	$2
OFFSET
	$1
`

type ListParams struct {
	Offset int32
	Limit  int32
}

func (q *Queries) List(ctx context.Context, arg ListParams) ([]*Department, error) {
	rows, err := q.db.Query(ctx, list, arg.Offset, arg.Limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []*Department{}
	for rows.Next() {
		var i Department
		if err := rows.Scan(
			&i.ID,
			&i.CreatedAt,
			&i.Slug,
			&i.Purpose,
			&i.SlackChannel,
		); err != nil {
			return nil, err
		}
		items = append(items, &i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
